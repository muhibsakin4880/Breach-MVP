// The accessStatus represents the current authentication status of the user in the application.
// It can have values such as 'authenticated', 'unauthenticated', and 'pending'.
// This handling ensures that the UI updates accordingly based on the authentication state.
// Additionally, it acts as a flag to control access to certain features within the app based on the user's login status.
// It's important to consistently manage the accessStatus to reflect any changes in user authentication, 
// ensuring that users cannot access protected routes without the necessary permissions.
