# API Service Layer

This directory will contain API integration services for backend communication.

Example structure:
- `api.ts` - Base API client configuration
- `auth.service.ts` - Authentication endpoints
- `datasets.service.ts` - Dataset management endpoints
- `ollama.ts` - Dataset chat integration against local Ollama (`/api/chat`)
- `types.ts` - TypeScript types for API responses
